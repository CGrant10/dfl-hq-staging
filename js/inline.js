// =====================================================================
// inline.js - editing where the content lives.
// =====================================================================
import { isAdmin, isMasterAdmin, hasPermission, insertRow, updateRow, deleteRow, selectOne } from "./supabase.js";
import { specFor } from "./sections.js";
import { fieldSet, setValue, readForm, fillOptionsFrom } from "./form.js";
import { hiddenCards, setCardHidden } from "./settings.js";
import { esc, toast } from "./ui.js";
const TABLE_PERMISSION={announcements:"announcements",events:"calendar",side_events:"calendar",polls:"polls",keepers:"keepers",rules:"rules",rule_categories:"rules",history:"history",members:"members",golf_outings:"golf",golf_participants:"golf",golf_teams:"golf",golf_rounds:"golf",golf_matches:"golf",golf_match_sides:"golf",golf_match_players:"golf",golf_match_scores:"golf",golf_scores:"golf",golf_courses:"golf",golf_course_holes:"golf",golf_event_codes:"golf",golf_profiles:"golf",golf_bag:"golf",golf_bag_visibility:"golf",arena_events:"broadcast",arena_participants:"broadcast",arena_results:"broadcast",broadcast_items:"broadcast"};
export function canEdit(table=null){if(!table)return isAdmin();const permission=TABLE_PERMISSION[table];return permission?hasPermission(permission):isMasterAdmin();}
const cardKey=(table,id)=>`${table}:${id}`;
export function isHidden(table,id){return hiddenCards().has(cardKey(table,id));}
export function visible(table,rows){if(canEdit())return rows||[];return(rows||[]).filter(r=>r?.id==null||!isHidden(table,r.id));}
export function hiddenClass(table,row){return row&&isHidden(table,row.id)?"is-hidden":"";}
export function editControls(table,row,{compact=false,del=true}={}){if(!canEdit(table))return"";const spec=specFor(table),name=spec?.label?spec.label(row):"",hidden=isHidden(table,row.id);return `<div class="inline-admin ${compact?"compact":""}">${hidden?`<span class="hidden-tag">Hidden</span>`:""}<button class="btn ghost small" data-inline-edit="${esc(table)}" data-id="${esc(row.id)}">Edit</button>${isMasterAdmin()?`<button class="btn ghost small" data-inline-hide="${esc(table)}" data-id="${esc(row.id)}" data-hide="${hidden?"0":"1"}">${hidden?"Show":"Hide"}</button>`:""}${del?`<button class="btn danger small" data-inline-del="${esc(table)}" data-id="${esc(row.id)}" data-label="${esc(name)}">Delete</button>`:""}</div>`;}
export function addControl(table,label,preset=null){if(!canEdit(table))return"";return `<button class="btn small inline-add" data-inline-add="${esc(table)}" ${preset?`data-preset="${esc(JSON.stringify(preset))}"`:""}>+ ${esc(label||"Add")}</button>`;}
export function wireInline(root,refresh){if(!root||!isAdmin())return;root.addEventListener("click",e=>{const add=e.target.closest("[data-inline-add]"),ed=e.target.closest("[data-inline-edit]"),del=e.target.closest("[data-inline-del]"),hide=e.target.closest("[data-inline-hide]");if(add){e.preventDefault();openEditor(add.dataset.inlineAdd,null,parsePreset(add.dataset.preset),refresh)}else if(ed){e.preventDefault();openEditor(ed.dataset.inlineEdit,ed.dataset.id,null,refresh)}else if(hide){e.preventDefault();toggleHidden(hide.dataset.inlineHide,hide.dataset.id,hide.dataset.hide==="1",refresh)}else if(del){e.preventDefault();removeRow(del.dataset.inlineDel,del.dataset.id,del.dataset.label,refresh)}});}
function saveError(err){const code=err?.code||"",msg=err?.message||"Save failed";if(code==="42501"||/row-level security/i.test(msg))return"Your commissioner account does not have permission for that change";if(code==="22007"||/invalid input syntax for type date/i.test(msg))return"That date is not valid";if(code==="23505")return"That already exists";if(/schema cache|does not exist/i.test(msg))return"That table is missing — run its schema SQL in Supabase";return msg;}
function parsePreset(raw){if(!raw)return null;try{return JSON.parse(raw)}catch{return null}}
async function toggleHidden(table,id,hide,refresh){if(!isMasterAdmin())return;try{await setCardHidden(cardKey(table,id),hide);toast(hide?"Hidden from members":"Visible again");refresh?.()}catch(err){toast(/app_settings/.test(err.message||"")?"Run settings_schema.sql in Supabase to hide cards":(err.message||"Could not change that"),true)}}
async function removeRow(table,id,label,refresh){if(!canEdit(table))return;const what=label?`"${label}"`:"this";if(!confirm(`Delete ${what}? This cannot be undone.`))return;try{await deleteRow(table,id);toast("Deleted");refresh?.()}catch(err){toast(saveError(err),true)}}
export async function openEditor(table,id,preset,refresh){if(!canEdit(table)){toast("You do not have permission to edit that",true);return}const spec=specFor(table);if(!spec){toast(`Nothing is set up to edit ${table}`,true);return}const creatingGolf=table==="golf_outings"&&!id;const createGolfNames=new Set(["name","event_date","event_time","notes"]);const fields=creatingGolf?spec.fields.filter(f=>createGolfNames.has(f.name)).map(f=>f.name==="name"?{...f,label:"Title"}:f.name==="notes"?{...f,label:"Notes (optional)"}:f.name==="event_time"?{...f,label:"Tee time (optional)",required:false}:{...f,required:true}):spec.fields;let row=null;try{if(id)row=await selectOne(table,id);await fillOptionsFrom(fields)}catch(err){toast(err.message||"Could not open the editor",true);return}const host=document.createElement("div");host.className="overlay";const golfMode=table==="golf_outings"?`<label for="i_event_type">Event type</label><select id="i_event_type" name="event_type"><option value="quick">Quick Round</option><option value="tournament_beta">Tournament Beta</option><option value="tournament">Tournament</option></select><p class="muted tiny">Create the event here. Course, golfers, holes, teams, matches and scoring are configured after you open it.</p>`:"";const heading=creatingGolf?"Create golf event":id?`Edit ${spec.singular}`:`Add ${spec.singular}`;host.innerHTML=`<div class="overlay-card wide" role="dialog" aria-modal="true"><h2>${esc(heading)}</h2><form id="inline-form">${golfMode}${fieldSet(fields,"i_")}<div class="row-end"><button type="button" class="btn ghost" data-close>Cancel</button><button type="submit" class="btn">Save</button></div></form></div>`;document.body.appendChild(host);const form=host.querySelector("#inline-form");fields.forEach(f=>{if(row)setValue(form,f,row[f.name]);else if(preset?.[f.name]!==undefined)setValue(form,f,preset[f.name]);else if(f.default!==undefined)setValue(form,f,f.default)});if(table==="golf_outings")form.elements.event_type.value=row?.event_type||preset?.event_type||"quick";const close=()=>{document.removeEventListener("keydown",onKey);window.removeEventListener("hashchange",close);host.remove()},onKey=e=>{if(e.key==="Escape")close()};document.addEventListener("keydown",onKey);window.addEventListener("hashchange",close);host.querySelector("[data-close]").addEventListener("click",close);host.addEventListener("click",e=>{if(e.target===host)close()});form.addEventListener("submit",async e=>{e.preventDefault();const btn=form.querySelector('button[type="submit"]');btn.disabled=true;try{const payload=readForm(form,fields);if(table==="golf_outings"){const courseId=form.querySelector('[name="course_id"]')?.value||"";if(!creatingGolf)payload.course_id=courseId||null;payload.event_type=form.elements.event_type?.value||"quick";if(creatingGolf){payload.holes=18;payload.status="setup"}}if(id)await updateRow(table,id,payload);else await insertRow(table,payload);toast(id?"Saved":"Added");close();refresh?.()}catch(err){toast(saveError(err),true);btn.disabled=false}});form.querySelector("input, textarea, select")?.focus();}
